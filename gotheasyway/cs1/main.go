package main	//every go program must have a package main.
import (	// import imports required files in to program for execution
	"fmt"	// importing format "fmt" file
)
func main() {	// main function definition(first to execute)
	fmt.Println("heloworld")	// println outputs the provided string to the user.
}
